#include <stdio.h>
int main()
{
    int a = 1, b = 2, c = 1, d;
    //  d = a || --b || c;
    // printf("a: %d,b: %d, c: %d, d: %d",a,b,c,d);
    //    d= a && --b || c;
    //    printf("a: %d,b: %d, c: %d, d: %d",a,b,c,d); //1111
    // d = --a && --b && c;
    // printf("a: %d,b: %d, c: %d, d: %d", a, b, c, d); //0210
    // c = --a || --b ;
    // printf("a: %d,b: %d, c: %d",a,b,c); //010
    // c= a || --b;
    // printf("a: %d,b: %d, c: %d",a,b,c);
    // c = --a || b;
    // printf("a: %d,b: %d, c: %d", a, b, c);
    // c = a && b;
    // printf("a: %d,b: %d, c: %d",a,b,c);
    // c = --a && b;
    // printf("a: %d,b: %d, c: %d",a,b,c);
    // c = --a && --b;
    // printf("a: %d,b: %d, c: %d",a,b,c); //010
    
}